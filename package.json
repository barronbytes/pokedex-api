{
  "_comments": {
    "init": "Created with 'npm init -y' to generate default package.json",
    "devDeps": "Dev dependencies installed with 'npm install -D typescript @types/node vitest'"
  },
  "name": "pokedex-api",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/main.js",
    "dev": "npx tsc && node dist/main.js",
    "test": "vitest --run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.10.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "zod": "^4.2.1"
  }
}
